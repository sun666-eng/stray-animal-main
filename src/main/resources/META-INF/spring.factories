# Spring Boot 3.4 still loads EnvironmentPostProcessor exclusively via spring.factories
# (NOT META-INF/spring/*.imports — that mechanism is for AutoConfiguration only).
org.springframework.boot.env.EnvironmentPostProcessor=\
com.example.component.ProdDeploymentEnvironmentPostProcessor
